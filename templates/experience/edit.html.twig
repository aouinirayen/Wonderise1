{% extends 'base.html.twig' %}

{% block title %}Edit Experience{% endblock %}

{% block body %}
    <h1>Edit Experience</h1>

    <p> {{ include('experience/_form.html.twig', {'button_label': 'Update'}) }} </p>

    <a href="{{ path('app_experience_index') }}" class="btn btn-primary">back to list</a>

    {{ include('experience/_delete_form.html.twig') }}
{% endblock %}