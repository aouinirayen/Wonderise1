<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Create an Account</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <style>
            body {
                min-height: 100vh;
                background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://images.unsplash.com/photo-1488646953014-85cb44e25828?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80') center/cover no-repeat;
            }
        </style>
    </head>
    <body>
<div class="min-vh-100 d-flex align-items-center py-5" style="background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://images.unsplash.com/photo-1488646953014-85cb44e25828?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80') center/cover no-repeat;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-5">
                <div class="card shadow-lg" style="background: rgba(255, 255, 255, 0.95);">
                    <div class="card-body p-5">
                        <h2 class="text-center mb-4 fw-bold">Create an Account</h2>
                        {{ form_start(form, {'attr': {'class': 'needs-validation', 'data-turbo': 'false'}}) }}
                            <div class="form-floating mb-3">
                                {{ form_widget(form.nom, {'attr': {'class': 'form-control' ~ (form.nom.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Enter your last name'}}) }}
                                {{ form_label(form.nom) }}
                                {% if not form.nom.vars.valid %}
                                    <div class="invalid-feedback d-block">{{ form_errors(form.nom) }}</div>
                                {% endif %}
                            </div>
                            <div class="form-floating mb-3">
                                {{ form_widget(form.prenom, {'attr': {'class': 'form-control' ~ (form.prenom.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Enter your first name'}}) }}
                                {{ form_label(form.prenom) }}
                                {% if not form.prenom.vars.valid %}
                                    <div class="invalid-feedback d-block">{{ form_errors(form.prenom) }}</div>
                                {% endif %}
                            </div>
                            <div class="form-floating mb-3">
                                {{ form_widget(form.username, {'attr': {'class': 'form-control' ~ (form.username.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Enter your username'}}) }}
                                {{ form_label(form.username) }}
                                {% if not form.username.vars.valid %}
                                    <div class="invalid-feedback d-block">{{ form_errors(form.username) }}</div>
                                {% endif %}
                            </div>
                            <div class="form-floating mb-3">
                                {{ form_widget(form.email, {'attr': {'class': 'form-control' ~ (form.email.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Enter your email'}}) }}
                                {{ form_label(form.email) }}
                                {% if not form.email.vars.valid %}
                                    <div class="invalid-feedback d-block">{{ form_errors(form.email) }}</div>
                                {% endif %}
                            </div>
                            <div class="form-floating mb-3">
                                {{ form_widget(form.password, {'attr': {'class': 'form-control' ~ (form.password.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Enter your password'}}) }}
                                {{ form_label(form.password) }}
                                {% if not form.password.vars.valid %}
                                    <div class="invalid-feedback d-block">{{ form_errors(form.password) }}</div>
                                {% endif %}
                            </div>
                            <div class="form-floating mb-4">
                                {{ form_widget(form.confirm_password, {'attr': {'class': 'form-control' ~ (form.confirm_password.vars.valid ? '' : ' is-invalid'), 'placeholder': 'Confirm your password'}}) }}
                                {{ form_label(form.confirm_password) }}
                                {% if not form.confirm_password.vars.valid %}
                                    <div class="invalid-feedback d-block">{{ form_errors(form.confirm_password) }}</div>
                                {% endif %}
                            </div>
                            <button type="submit" class="btn btn-primary w-100 py-3 mb-3 text-uppercase fw-bold">Create Account</button>
                        {{ form_end(form) }}
                        <div class="text-center">
                            <p class="mb-0">Already have an account? <a href="{{ path('app_login') }}" class="text-primary fw-bold">Sign In</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>