{% extends 'base.html.twig' %}

{% block title %}Art Pieces{% endblock %}

{% block stylesheets %}
    <style>
        .blog-item {
            display: flex;
            flex-direction: row;
            align-items: stretch;
            width: 50%;
        }
        .blog-img {
            flex: 1;
            width: 50%;
        }
        .blog-content {
            flex: 2;
            display: flex;
            flex-direction: column;
            justify-content: center;
            width: 50%;
        }
    </style>
{% endblock %}

{% block body %}
    <!-- Art Pieces Start -->
    <div class="container-fluid blog py-5">
        <div class="container py-5">
            <div class="mx-auto text-center mb-5" style="max-width: 900px;">
                <h5 class="section-title px-3">Art Pieces</h5>
                <h1 class="mb-4">Explore Our Art Pieces</h1>
                <p class="mb-0">Discover the beauty and creativity of art pieces from around the world. From classic paintings to modern sculptures, there's something for everyone to admire.</p>
            </div>
            <div class="d-flex justify-content-end mb-3">
                <a href="{{ path('app_art_new') }}" class="btn btn-primary">Create new Art</a>
            </div>
            <div class="row g-4 justify-content-center">
                {% for art in arts %}
                    <div class="col-md-12 d-flex">
                        <div class="blog-item hover-card w-100">
                            <div class="blog-img">
                                <div class="blog-img-inner">
                                    <img class="img-fluid w-100 rounded-top" src="{{ art.img }}" alt="{{ art.name }}" style="height: 200px; object-fit: cover;">
                                    <div class="blog-icon">
                                        <a href="{{ path('app_art_show', {'id': art.id}) }}" class="my-auto">
                                            <i class="fas fa-link fa-2x text-white"></i>
                                        </a>
                                        <a href="{{ path('app_art_edit', {'id': art.id}) }}" class="btn btn-secondary rounded-pill py-2 px-4">Update</a>
                                        <form method="post" action="{{ path('app_art_delete', {'id': art.id}) }}" class="d-inline">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ art.id) }}">
                                            <button class="btn btn-secondary rounded-pill py-2 px-4">Delete</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                            <div class="blog-content border border-top-0 rounded-bottom p-4">
                                <p class="mb-3">Art Piece: {{ art.name }}</p>
                                <a href="{{ path('app_art_show', {'id': art.id}) }}" class="h4">{{ art.name }}</a>
                                <p class="my-3">Explore the unique beauty and creativity of {{ art.name }}.</p>
                                <a href="{{ path('app_art_show', {'id': art.id}) }}" class="btn btn-primary rounded-pill py-2 px-4">View Details</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
    <!-- Art Pieces End -->
{% endblock %}