{% extends 'layout.html.twig' %}

{% block title %}🆕 Nouvelle Réponse{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="card shadow-lg border-dark rounded-4 p-4">
            <div class="card-header bg-success text-white text-center py-3">
                <h1 class="fw-bold text-uppercase">
                    <i class="fas fa-plus-circle me-2"></i> Créer une Nouvelle Réponse
                </h1>
            </div>

            <div class="card-body">
                {{ include('back_office/reponse/_form.html.twig') }}
            </div>

            <div class="text-center mt-3">
                <a href="{{ path('app_reponse_index') }}" class="btn btn-secondary btn-lg">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>

    {# Bootstrap & FontAwesome #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
{% endblock %}
