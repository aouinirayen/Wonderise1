{% extends 'layout.html.twig' %}

{% block title %}📄 Détails Réponse{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="card shadow-lg border-dark rounded-4">
            <div class="card-header bg-info text-white text-center py-3">
                <h1 class="fw-bold text-uppercase">
                    <i class="fas fa-eye me-2"></i> Détails de la Réponse
                </h1>
            </div>

            <div class="card-body p-4">
                <h4 class="fw-bold text-primary">📌 Informations sur la Réponse</h4>
                <table class="table table-bordered border-dark">
                    <tbody>
                        <tr>
                            <th class="bg-light">ID Réponse</th>
                            <td>{{ reponse.id }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">Réponse</th>
                            <td>{{ reponse.reponse }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">Date</th>
                            <td>{{ reponse.Date ? reponse.Date|date('Y-m-d') : '-' }}</td>
                        </tr>
                    </tbody>
                </table>

                <h4 class="fw-bold text-danger mt-4">📌 Informations sur la Réclamation</h4>
                <table class="table table-bordered border-dark">
                    <tbody>
                        <tr>
                            <th class="bg-light">ID Réclamation</th>
                            <td>{{ reponse.reclamation.id }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">Objet</th>
                            <td>{{ reponse.reclamation.Objet }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">Description</th>
                            <td>{{ reponse.reclamation.Description }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">Statut</th>
                            <td>
                                <span class="badge 
                                    {% if reponse.reclamation.status.value == 'traitée' %} bg-success
                                    {% elseif reponse.reclamation.status.value == 'rejetée' %} bg-danger
                                    {% elseif reponse.reclamation.status.value == 'en cours' %} bg-warning text-dark
                                    {% elseif reponse.reclamation.status.value == 'envoyée' %} bg-secondary {% endif %}">
                                    {{ reponse.reclamation.status.value }}
                                </span>
                            </td>
                        </tr>
                    </tbody>
                </table>

                <div class="text-center mt-4">
                    <a href="{{ path('app_reponse_index') }}" class="btn btn-secondary btn-lg">
                        <i class="fas fa-arrow-left"></i> Retour à la liste
                    </a>
                    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}" class="btn btn-warning btn-lg">
                        <i class="fas fa-edit"></i> Modifier
                    </a>
                    
                </div>
            </div>
        </div>
    </div>

    {# Bootstrap & FontAwesome #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
{% endblock %}
